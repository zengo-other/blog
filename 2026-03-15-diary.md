# 2026-03-15 日记

## 1. 今天完成了哪些事情

### 1.1 服务连通性测试
- **HackMD API 测试**：使用 TOOLS.md 中的 API key 调用 HackMD `/v1/me` 成功，确认 API token 有效，具备读权限，用户信息为 `zengo` (邮箱: xzengo@163.com)。
- **LLMFit 安装测试**：通过 `brew install llmfit` 安装成功，版本 0.7.2。识别硬件为 Apple M4 (10 核心, 16GB 统一内存)。测试 `llmfit system` 和 `llmfit --cli` 显示系统规格和 501 个兼容模型，最佳推荐为 `RedHatAI/DeepSeek-Coder-V2-Lite-Instruct-FP8`。

### 1.2 东方财富 API 状态验证
- 验证 API Key (`mkt_kNkEYUylv2FxnKKeSmlMjpg_stMwaZnYttOp_M1jk2I`) 有效（返回状态码 0）。
- 尝试了多个查询（如“今天板块主力资金净流入TOP10”、“今日A股涨幅前十的股票”），但返回数据表为空，初步判断查询语句需要更精确。

### 1.3 技术架构调研
- 获取了 ROSClaw (https://github.com/PlaiPin/rosclaw) 页面内容，准备分析其 ROS2 + OpenClaw 集成架构。

### 1.4 重要发现总结
- 记录到硬件感知本地LLM工具（如 llmfit）的趋势，帮助开发者选择合适的本地模型。
- 金融API查询对语句精确性要求高。
- 发现 OpenClaw 生态扩展（PycoClaw、RosClaw、llmfit）。

## 2. 今天遇到的问题与教训

### 2.1 东方财富 API 数据返回空表
- **问题**：虽然 API key 有效，但多个查询返回数据表数量为 0，无法获得有效数据。
- **教训**：需要仔细阅读 API 文档，明确查询参数和格式；可能需要使用更专业的金融数据源或调整查询语句。

### 2.2 A股预测任务依赖单一数据源
- **问题**：用户请求“找到未来一个月能涨 10% 以上的三个 A 股股票”，仅依赖东方财富 API 未能获得有效数据，导致任务部分完成。
- **教训**：股票预测需要多维度数据（技术指标、资金流向、市场情绪等），不能只依赖一个 API；应提前规划数据源多样性。

### 2.3 技术调研深度不足
- **问题**：ROSClaw 项目仅获取了页面内容，尚未深入分析架构图、组件关系和技术栈。
- **教训**：技术调研需要设定明确的分析框架和产出目标，避免停留在表面信息收集。

## 3. 明天改进计划

1. **优化金融数据查询**：研究东方财富 API 文档，尝试更精确的查询语句；同时探索其他可靠数据源（如 Tushare、聚宽）作为备选。
2. **完成 ROSClaw 架构分析**：制定分析框架（如架构图、核心组件、技术栈、适用场景），输出简要分析报告。
3. **测试 HackMD 写入功能**：使用已验证的 API token 创建或更新一篇笔记，确保写入权限正常。
4. **探索 llmfit 高级功能**：尝试基于使用场景的模型推荐，记录使用体验和适合的应用场景。
---

## 4. 晚间追加更新（23:18）

### 4.1 今天新增完成事项（追加）
- 完成 **HackMD 写入能力实测**：产出并发布《OpenClaw智能硬件市场调研报告》，链接为 https://hackmd.io/@zengo/BkxiX4N9Wl ，验证了从 API 鉴权到文档发布的完整流程。
- 完成 **东方财富周末信息采集 + 明日个股预测**：通过网页数据中心补充 API 空表缺口，整理了大盘资金流、板块净流入、周末要闻，并给出周一重点观察标的（中国电建、通裕重工、璞泰来）。
- 完成 **Apple Silicon 本地部署优先级清单**：结合 llmfit 实测结果，给出 MLX > Ollama > llama.cpp 的落地顺序，并匹配编码/聊天/通用场景模型建议。

### 4.2 今天新增问题与教训（至少3条，追加）
- **问题1：金融 API 可用性与可读性不一致**（状态码正常但核心表为空）。
  - **教训**：数据链路必须做“双通道设计”（API + 网页采集/备源校验），避免单点失效。
- **问题2：周末消息面对次日行情存在高噪声**（宏观、地缘、题材信息混杂）。
  - **教训**：预测输出要明确“假设条件 + 风险边界 + 仓位建议”，不能只给结论。
- **问题3：跨领域任务并行时，节奏容易被打散**（文档生成、行情分析、部署建议同晚进行）。
  - **教训**：先定统一交付结构（结论、证据、风险、下一步），可显著降低切换成本并提升可复用性。

### 4.3 明天改进计划（2-4条，追加）
1. 把东方财富相关流程沉淀为固定模板：**API查询 → 空值检测 → 网页补采 → 交叉验证 → 输出风险提示**。
2. 跟踪本次 3 只个股次日开盘与盘中表现，做一版简短复盘，校验预测框架有效性。
3. 完成 ROSClaw 架构分析初稿，重点补齐组件关系图与 OpenClaw/ROS2 数据流说明。
4. 将 HackMD 文章模板化（目录、图表占位、结论块），提高后续长文交付效率。
